{
  "name": "typescript-react-node-app",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "8.9.x"
  },
  "scripts": {
    "start": "node ./build/index.js",
    "dev-client": "cd fillfront && npm run dev",
    "dev-server": "nodemon --exec ./node_modules/.bin/ts-node -- ./server/index.ts",
    "test": "cd fillfront && npm test",
    "heroku-postbuild": "tsc & cd fillfront/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "fillfront/node_modules"
  ],
  "dependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.1.2",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^1.17.4",
    "ts-node": "^6.0.3",
    "typescript": "^3.1.6"
  }
}
